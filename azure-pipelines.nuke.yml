# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelinesSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

#
parameters:
  Configuration: 'Release'
  Verbosity: 'Normal'
steps:
  - pwsh: nuke BuildVersion --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'BuildVersion'
  - pwsh: nuke Clean --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'Clean'
  - pwsh: nuke Restore --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: '📪 Restore'
  - pwsh: nuke InstallHomebrew --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'InstallHomebrew'
  - pwsh: nuke Homebrew --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'Homebrew'
  - pwsh: nuke InstallFastlane --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'InstallFastlane'
  - pwsh: nuke ModifyInfoPlist --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'ModifyInfoPlist'
  - pwsh: nuke FastlaneMatch --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'FastlaneMatch'
  - pwsh: nuke Fastlane --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'Fastlane'
  - pwsh: nuke ArchiveIpa --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'ArchiveIpa'
  - pwsh: nuke XamariniOS --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'XamariniOS'
  - pwsh: nuke Default --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'Default'
  - pwsh: nuke AzurePipelines --skip --configuration '${{ parameters.Configuration }}' --verbosity '${{ parameters.Verbosity }}'
    displayName: 'AzurePipelines'
