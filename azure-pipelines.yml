# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - main

stages:
  - stage: macOS_11
    displayName: 'macOS-11'
    dependsOn: [  ]
    pool:
      vmImage: 'macOS-11'
    jobs:
      - job: InstallDotNet
        displayName: 'InstallDotNet'
        dependsOn: [  ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh InstallDotNet --skip'
      - job: Clean
        displayName: 'Clean'
        dependsOn: [ InstallDotNet ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Clean --skip'
      - job: Restore
        displayName: 'Restore'
        dependsOn: [ Clean, Clean ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Restore --skip'
      - job: Boots
        displayName: 'Boots'
        dependsOn: [ Clean, Restore ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Boots --skip'
      - job: ModifyInfoPlist
        displayName: 'ModifyInfoPlist'
        dependsOn: [ Restore ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh ModifyInfoPlist --skip'
      - job: Build
        displayName: 'Build'
        dependsOn: [ Restore, Restore, Boots, ModifyInfoPlist ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Build --skip'
      - job: Test
        displayName: 'Test'
        dependsOn: [ Build ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Test --skip'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ Test, Build ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Pack --skip'
      - job: XamariniOS
        displayName: 'XamariniOS'
        dependsOn: [ Restore, Boots, ModifyInfoPlist, Build, Pack ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh XamariniOS --skip'
      - job: Default
        displayName: 'Default'
        dependsOn: [ XamariniOS ]
        steps:
          - checkout: self
            fetchDepth: 0
          - task: Cache@2
            displayName: Cache (nuke-temp)
            inputs:
              key: $(Agent.OS) | nuke-temp | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuke-temp
              path: .nuke/temp
          - task: Cache@2
            displayName: Cache (nuget-packages)
            inputs:
              key: $(Agent.OS) | nuget-packages | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS) | nuget-packages
              path: $(HOME)/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.sh Default --skip'
